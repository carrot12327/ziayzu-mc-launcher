<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Set primary, dark, and accent colors using your orange definitions -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Optionally set background and button colors if you have them in colors.xml -->
        <item name="android:windowBackground">@color/background_app</item>
        <item name="android:statusBarColor">@color/background_status_bar</item>
        <item name="android:buttonStyle">@style/OrangeButton</item>
        <!-- Material component support (optional) -->
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorOnBackground">@color/colorTextPrimary</item>
    </style>

    <!-- Optional: orange button style if you want your buttons orange -->
    <style name="OrangeButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/colorButtonNormal</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Optional: toolbar color -->
    <style name="OrangeToolbar" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>
</resources>
